name: Deployment-Dev

on:
  workflow_run:
    workflows: 
      - Docker-Dev
    types:
      - completed

jobs:
  deploy-1:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    steps:
      - name: Call kb-api-dev endpoint
        run: |
          curl -X POST http://restart.kanpaibook.com/update/kb-api-dev -H "API-Key: ${{ secrets.WEBHOOK_API_KEY }}"
